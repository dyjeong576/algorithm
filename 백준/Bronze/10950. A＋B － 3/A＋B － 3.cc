#include <bits/stdc++.h>
using namespace std;

int main(){
	
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);	cout.tie(NULL); 
    
    int num, x, y;
    
    cin >> num;
    
    while(num--){
    	cin >> x >> y;
    	cout << x + y << endl;
	}
    
	
	return 0;
    
}